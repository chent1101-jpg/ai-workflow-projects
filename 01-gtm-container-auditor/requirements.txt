# No dependencies. Parser, rules, and judge run on the standard library —
# `python3 -m src.rules data/sample_container.json` works from a clean clone
# with zero installs. The judge layer additionally needs OPENROUTER_API_KEY.
